// Fetches BPM, energy, key per track.
// Implemented by: feature/services agent
export function useAudioFeatures(_trackId: string | null) {
  return { features: null, loading: false, error: null };
}
